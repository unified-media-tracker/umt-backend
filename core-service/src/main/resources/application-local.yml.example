spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/core_db
    username: core
    password: core

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: validate

  rabbitmq:
    host: localhost
    port: 5672
    username: umt
    password: umt

tmdb:
  api-key:   # get at https://www.themoviedb.org/settings/api