spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      server:
        webflux:
          globalcors:
            cors-configurations:
              '[/**]':
                allowedOrigins: "${FRONTEND_ORIGIN:http://localhost:3000}"
                allowedMethods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
                allowedHeaders: "*"
                allowCredentials: true
          routes:
            - id: core-service-route
              uri: http://${CORE_SERVICE_HOST:localhost}:${CORE_SERVICE_PORT:8081}
              predicates:
                - Path=/api/core/**

            - id: ai-analyser-route
              uri: http://${AI_ANALYSER_HOST:localhost}:${AI_ANALYSER_PORT:8082}
              predicates:
                - Path=/api/analyser/**

server:
  port: 8080